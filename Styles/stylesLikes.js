import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  manhuaCard: {
    aspectRatio: 1 / 1.4,
    /*1.4:1 é proporção padrão de capas de 'gibi'*/
    height: 200,
    borderRadius: 10,
    justifyContent: 'center',
    textAlign: 'center',
    margin: 6,
  },

});
