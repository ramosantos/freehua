import {DarkTheme} from '@react-navigation/native';

export default FreeSpace = {
    ...DarkTheme,
    colors: {
        ...DarkTheme.colors,
        card: '#144272',
        background: '#ffff',
        border: '#205295',
        primary: '#FF914D',
    }
}
